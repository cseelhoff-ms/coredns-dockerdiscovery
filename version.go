package dockerdiscovery

// pluginVersion is bumped per source change so operators can confirm
// the running binary matches the latest deploy by looking at the
// startup banner. Increment this whenever you make a behavior change
// you'd want to verify in someone else's logs.
const pluginVersion = "0.6.0-dnssync-debug"
